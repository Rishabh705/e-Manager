import { Allerta_Stencil, Zen_Dots, Quicksand, Inter, Mulish } from 'next/font/google'
 
export const allerta = Allerta_Stencil({
  subsets: ['latin'],
  weight: ['400']
})

export const quicksand = Quicksand({
  subsets:['latin'],
  weight:['500']
})
 
export const zen_dots = Zen_Dots({
  subsets: ['latin'],
  display: 'swap',
  weight: ['400']
})

export const inter = Inter({
  subsets: ['latin'],
  display: 'swap',
  weight: ['400']
})

export const mulish = Mulish({
  subsets:['latin'],
  weight:['400']}
)